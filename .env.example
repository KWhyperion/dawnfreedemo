we need # Public URL of the lead-capture endpoint (a Lambda deployed OUTSIDE this repo
# that relays the lead to us via SES). The browser POSTs JSON here; it never
# holds AWS credentials and never calls SES directly.
VITE_LEAD_ENDPOINT=https://rfrjuajifviyv6ab75odlvww740bhtmz.lambda-url.eu-west-1.on.aws/
